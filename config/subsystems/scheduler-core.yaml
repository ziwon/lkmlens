# Patterns have been observed in the indexed kvm and bpf corpora.
layer: CPU scheduler core
file_patterns:
  - "kernel/sched/**"
stakeholders:
  - sched_ext BPF-scheduler authors
  - Cloud/hypervisor CPU-scheduling teams
