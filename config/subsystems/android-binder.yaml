# Patterns have been observed in the indexed rust-for-linux corpus (rust_binder).
layer: Android IPC (binder) driver
file_patterns:
  - "drivers/android/**"
stakeholders:
  - Android platform team
  - AOSP kernel integrators
