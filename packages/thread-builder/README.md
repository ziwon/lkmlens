# @lkmlens/thread-builder (planned)

Reply-tree and patch-series reconstruction from `Message-ID`/`In-Reply-To`/
`References` headers, including cover-letter recognition and `x/y` patch
index parsing for v1/v2/v3 revision grouping.

Not yet implemented. See the project planning doc (kept locally, not published)
section 20 ("Thread reconstruction ambiguity") and Phase 2 (Patch
intelligence).
