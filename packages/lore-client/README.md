# @lkmlens/lore-client (planned)

Fetches message metadata and raw messages from lore.kernel.org's public-inbox
endpoints (per-list Atom feeds, `git` grokmirror clones, or `/raw` message
fetches by Message-ID).

Not yet implemented. See the project planning doc (kept locally, not published)
section 7.3 (Collection) and section 24 item 8 ("Implement one-source
ingestion for a narrow test list").
