# @lkmlens/ui (planned)

Shared UI components (search box, topic badges, result cards, thread views)
once `apps/web` has more than one page consuming them. `apps/web` currently
owns its own minimal components directly.
