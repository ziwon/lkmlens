# indexer (planned)

Queue consumer that runs the thread-indexing Workflow: parse MIME, normalize,
persist canonical records, rebuild affected threads, classify topics, and
refresh the FTS index. See the project planning doc (kept locally, not published)
section 7.5.

Not yet implemented — Phase 1.
